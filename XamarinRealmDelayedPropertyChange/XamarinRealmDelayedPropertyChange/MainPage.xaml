<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XamarinRealmDelayedPropertyChange.MainPage">

    <StackLayout>
        <Entry Text="{Binding Value, Mode=TwoWay}" />
        <Button Text="Change persistent value" Clicked="ChangePersistentValue_Clicked" />

        <Entry Text="{Binding TransientValue, Mode=TwoWay}" />
        <Button Text="Change transient value" Clicked="ChangeTransientValue_Clicked" />
    </StackLayout>

</ContentPage>

